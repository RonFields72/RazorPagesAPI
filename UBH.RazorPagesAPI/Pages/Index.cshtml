@page
@model IndexModel
@{
    ViewData["Title"] = "May the Force Be With You!";
}

@section scripts {
    <script>
        $(document).ready(function () {
            var starWarsNameDisplay = $('#starWarsNameDisplay');
            getStarWarsName();

            function getStarWarsName() {
                $.ajax({
                    url: '/StarWarsFinder',
                    data: {
                    }
                })
                    .done(function (result) {
                        $(starWarsNameDisplay).text(result);
                    });
            }
        });
    </script>

}

<div class="text-center" style="color:darkcyan">
    <h6>This page calls another Razor Page that returns JSON formatted result. It basically serves as a mini-API...</h6>
</div>

<div class="text-center">
    <h4>A long time ago, in a galaxy far, far away...</h4>
</div>

<div>
    <p style="text-align: center;">
        These aren't the droids you're looking for, <span style="font-weight:700;" id="starWarsNameDisplay"></span>!
    </p>
</div>
